import React from 'react';
import Loading from 'react-loading-components';
 
const Example = () => (
  <Loading type='spinning_circles' width={30} height={30} fill='gray' />
);
 
export default Example;